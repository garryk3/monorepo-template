import { h } from 'preact';
import { memo } from 'preact/compat';

export const ExamplePage = memo(() => {
    return (
        <div>
            EXAMPLE PAGE from Example App
        </div>
    );
});